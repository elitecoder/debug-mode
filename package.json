{
  "name": "debug-mode",
  "version": "0.1.0",
  "private": true,
  "description": "Hypothesis-driven debugging skill for Claude Code with HTTP ingest for browser and mobile.",
  "scripts": {
    "test": "node --test test/ingest_server.test.js",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-lines=100 --test-coverage-functions=100 --test-coverage-branches=95 --test-coverage-exclude='test/**' --test-coverage-exclude='**/cmux-claude-node-options/**' test/ingest_server.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
